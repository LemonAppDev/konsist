# Architecture 2

Here we're testing situation where Presentation layer depends on Domain one. See below diagram:

```mermaid
%%{init: {'theme': 'forest'}}%%
flowchart LR
    Presentation --> Domain
```
