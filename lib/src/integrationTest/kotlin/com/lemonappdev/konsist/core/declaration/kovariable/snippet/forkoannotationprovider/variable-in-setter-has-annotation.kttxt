import com.lemonappdev.konsist.testdata.SampleAnnotation

var sampleProperty: String = "text"
    set(value) {
        @SampleAnnotation
        val sampleVariable = ""

        if (true) field = value
    }
