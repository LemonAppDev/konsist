package com.lemonappdev.konsist.api.architecture

/**
 * Represents a combination of a [KoArchitecture] and its associated [com.lemonappdev.konsist.api.container.koscope.KoScope].
 *
 * This interface serves as a convenient way to bundle an architecture instance
 * and its corresponding scope together as a single entity.
 */
interface ArchitectureScope
